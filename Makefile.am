# pkgconfigdir = $(libdir)/pkconfig
# pkgconfig_DATA = libccp4c.pc libccp4f.pc

if FORTRAN_LIB
  FORTRAN_LIB = fortran
endif

SUBDIRS = ccp4 $(FORTRAN_LIB) data
DIST_SUBDIRS = ccp4 fortran data

EXTRA_DIST = AUTHORS COPYING ChangeLog INSTALL NEWS README
